# Vernex agent — Anthropic API key for /api/chat Vercel Function.
# Get from https://console.anthropic.com/settings/keys
# Set in Vercel Project Settings → Environment Variables → Production.
ANTHROPIC_API_KEY=

# Supabase — lead capture from Terminal CTA (site_leads table).
# Public values (prefix VITE_ = exposed to client bundle, expected).
VITE_SUPABASE_URL=https://yachdnnoudjxdxflcjrg.supabase.co
VITE_SUPABASE_ANON_KEY=
